package se.gustavkarlsson.skylight.android.initializers

import androidx.appcompat.app.AppCompatDelegate

internal fun initDarkMode() {
    AppCompatDelegate.setDefaultNightMode(AppCompatDelegate.MODE_NIGHT_YES)
}
